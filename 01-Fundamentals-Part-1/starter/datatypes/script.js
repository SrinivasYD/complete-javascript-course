// 2. Datatypes

// number - float by default
let num = 25;

// string
let lastName = "Adolf";

// Boolean
let checked = true;

// Undefined value
let undefined_value;

// Null value, BigInt & Symbol

// 3. Using let,const & var
let age;
age = 30; // We can assign the value after it is declared as let
age = "Thirty One"; // We can change the type of the variable

const birthYear = 1990;
// birthYear = 1991; Is not possible because we declared it as constant
